
<div class="container">
  <h6>Create a comment</h6>
  <%= simple_form_for(comment, remote: true) do |f| %>
  <div class="container">
    <%= f.input :content %>
    <%= f.button :submit %>
  </div>
  <% end %>
</div>

